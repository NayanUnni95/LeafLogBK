from app.main import app  # This will be picked up by Vercel

# Optional: still allow local dev
if __name__ == "__main__":
    import uvicorn
    import os
    port = int(os.environ.get("PORT", 8000))
    uvicorn.run(app, host="0.0.0.0", port=port)
